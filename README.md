Windows Setup:
==============

download and install GIT bash from the following link:

https://git-scm.com/install/windows

run this command on bash:

git clone -b dup_ship https://github.com/sowaan/invoice-utility.git

